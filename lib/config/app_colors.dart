import 'package:flutter/material.dart';

class AppColors {
  // Main colors
  static const primary = Colors.blue;
  static const secondary = Colors.orange;
  static const recording = Colors.red;

  // Background colors
  static const scaffoldBackground = Colors.white;
  static final gradientStart = Colors.blue.shade300;
  static final gradientEnd = Colors.purple.shade300;

  // Text colors
  static const textPrimary = Colors.black87;
  static const textSecondary = Colors.grey;
  static const textLight = Colors.white;

  // UI element colors
  static final cardBackground = Colors.white;
  static final shadowColor = Colors.black.withOpacity(0.2);

  // Status colors
  static const success = Colors.green;
  static const error = Colors.red;
  static const warning = Colors.amber;
  static const info = Colors.blue;
}